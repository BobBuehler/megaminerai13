using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

public static class Solver
{
    public static void MoveAndAttack(IEnumerable<Point> attackers, IEnumerable<Point> targets)
    {
        foreach (var a in attackers)
        {
            Droid attacker = null; // TODO
            
        }
    }
}
